name: Build unypkg Base System

on: workflow_dispatch

jobs:
  build-unypkg-system:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: ls -lh
      - run: base/unypkg-base-build.sh
